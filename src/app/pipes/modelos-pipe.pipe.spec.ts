import { ModelosPipePipe } from './modelos-pipe.pipe';

describe('ModelosPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ModelosPipePipe();
    expect(pipe).toBeTruthy();
  });
});
